# Lyon Research Examples

This directory contains research examples for `lyon` and is organized for multiple sample programs.

Structure:
- `examples/`: example binaries for lyon experiments
- `src/`: shared code and helper utilities used by examples

Run the minimal example:

```bash
cd research/lyon
cargo run --example minimal
```

The minimal example builds a simple triangle path and tessellates it with `lyon`.
