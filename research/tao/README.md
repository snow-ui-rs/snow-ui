# Tao Research Examples

This directory contains research examples for `tao` and is organized for multiple sample programs.

Structure:
- `examples/`: multiple example binaries for tao experiments
- `src/`: shared code and helper utilities used by examples

Run the minimal example:

```bash
cd research/tao
cargo run --example minimal
```

The minimal example creates a window and exits when closed or when Escape is pressed.
